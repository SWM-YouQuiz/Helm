apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{.Chart.Name}}-{{.Values.quizItService.quiz.name}}-db
spec:
  serviceName: {{ .Values.quizItService.quiz.db.label }}
  replicas: {{ .Values.quizItService.quiz.db.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.quizItService.quiz.db.label }}
  template:
    metadata:
      labels:
        app: {{ .Values.quizItService.quiz.db.label }}
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: mongo
        image: mongo:6.0.9-rc1
        ports:
          - containerPort: 27017
        volumeMounts:
          - name: quizit-quiz-prod
            mountPath: /data/db
      # volumes:
      # - name: quizit-quiz-db
      #   persistentVolumeClaim:
      #     claimName: quizit-quiz-prod-pvc
    # spec:
    #   containers:
    #   - name: my-container
    #     image: my-image
    #     volumeMounts:
    #     - name: my-volume
    #       mountPath: /path/to/mount
    #   volumes:
    #   - name: my-volume
    #     persistentVolumeClaim:
    #       claimName: quizit-quiz-prod-pvc
  # volumeClaimTemplates:
  # - metadata:
  #     name: quizit-quiz-prod-pvc
  #   spec:
  #     storageClassName: {{ .Values.quizItService.quiz.db.storage.className }}
  #     accessModes: 
  #       - {{ .Values.storage.accessMode }}
  #     resources:
  #       requests:
  #         storage: {{ .Values.quizItService.quiz.db.storage.request }}